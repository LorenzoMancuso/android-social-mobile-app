<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.rathings">

    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/default_profile_image"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/default_profile_image_round"
            android:supportsRtl="true"
            android:theme="@style/MaterialTheme"
            tools:ignore="GoogleAppIndexingWarning"
            tools:targetApi="28">

        <provider
                android:name="androidx.core.content.FileProvider"
                android:authorities="${applicationId}.provider"
                android:exported="false"
                android:grantUriPermissions="true">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".User.FollowListActivity">
        </activity>
        <activity android:name=".Card.MultimediaActivity">
        </activity>
        <activity android:name=".Card.EditCardActivity"/>
        <activity android:name=".Tab.TabsActivity"/>
        <activity android:name=".Card.DetailedCardActivity"/>
        <activity android:name=".Card.NewCardActivity"/>
        <activity android:name=".User.ProfileActivity"/>
        <activity android:name=".User.ModifyAccountActivity"/>
        <activity android:name=".HomeActivity"/>
        <activity
                android:name=".LoginActivity"
                android:label="@string/title_activity_login"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>