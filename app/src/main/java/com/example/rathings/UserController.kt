package com.example.rathings

object UserController {
    /**call firebase utils for get, update and delete data about users.
     * Every time we get JSON data from Firebase, the controller parse it
     * to an Object of type User*/
}